name: Update Schindler services image
on:
  push:
    paths:
      - '.github/workflows/fix-hero-medical.yml'
permissions:
  contents: write
jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Replace services image with mask ventilation
        run: |
          python - <<'PY'
          from pathlib import Path
          p=Path('index.html')
          s=p.read_text(encoding='utf-8')
          old='https://christiandiebner86-cpu.github.io/halle-website-test/hero-trachealkanuele-beatmung.png'
          new='leistungen-maskenbeatmung.png'
          if old not in s:
              raise SystemExit('current services image not found')
          s=s.replace(old,new,1)
          s=s.replace('alt="Beatmungs- und Trachealkanülenversorgung"','alt="Außerklinische Intensivpflege mit nicht-invasiver Maskenbeatmung"',1)
          p.write_text(s,encoding='utf-8')
          PY
      - name: Commit patch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add index.html
          git commit -m 'Use mask ventilation image for services'
          git push
